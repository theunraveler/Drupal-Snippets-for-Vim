
/**
 * Implementation of hook_block()
 */
function `Filename()`_block($op = 'list', $delta = 0, $edit = array()) {
	switch ($op) {
		case 'list':
			${1}
		case 'configure':
		case 'save':
		case 'view':
	}
}

